<!DOCTYPE html>
<html>
  <head>
    <style>
      body {
        font-family: Arial, sans-serif;
        color: #333;
        line-height: 1.6;
      }
      .container {
        padding: 20px;
        border: 1px solid #ddd;
        border-radius: 8px;
        max-width: 600px;
        margin: 0 auto;
        background-color: #f9f9f9;
      }
      .header {
        color: #d9534f;
        text-align: center;
      }
      .details {
        background-color: #fff;
        padding: 15px;
        border-radius: 4px;
        margin-top: 20px;
        border-left: 4px solid #d9534f;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h2 class="header">Scraping Error Alert</h2>
      <p>An error occurred while scraping the provider: <strong><%= provider %></strong>.</p>
      
      <div class="details">
        <p><strong>Error Message:</strong></p>
        <pre><%= error %></pre>
      </div>

      <p>Please check the server logs for more details.</p>
    </div>
  </body>
</html>
